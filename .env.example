# Example environment variables for yadot-web
# Copy this file to .env.local for local development

# Optional: Set a default reference date (format: YYYY-MM-DD)
# This will be used if no cookie is set
# NEXT_PUBLIC_REFERENCE_DATE=2000-01-01

# Node environment (production, development, test)
NODE_ENV=production
